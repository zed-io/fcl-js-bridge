{
  "name": "flow-js-bridge",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf ../dist && parcel build index.js --no-source-maps -o fcl-min.js -d ./",
    "watch": "rm -rf ../dist && parcel watch index.js -o fcl-min.js -d ./",
    "lint": "eslint . --fix",
    "test": "jest"
  },
  "dependencies": {
    "@onflow/fcl": "^0.0.76",
    "@onflow/types": "^0.0.5",
    "buffer": "^5.6.0",
    "events": "^3.2.0",
    "isutf8": "^3.1.1"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "eslint": "^5.16.0",
    "jest": "^23.6.0",
    "lodash": "^4.17.21",
    "parcel-bundler": "^1.12.4"
  }
}